cv:
  name: Yoongon Kim
  label: B.S. Student in Electrical and Computer Engineering
  email: yooongonkim@gmail.com
  location: Seoul, South Korea

  # The CV page is set to "PDF download only" (see `cv_pdf` in _pages/cv.md), so the
  # structured demo sections were removed on purpose. If you ever want the
  # rendered/structured CV instead of (or alongside) the PDF, add sections below —
  # e.g. Education, Experience, Awards, Skills. See the al-folio docs for the schema.
  sections: {}
